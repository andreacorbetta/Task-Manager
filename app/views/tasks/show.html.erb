<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>
<br>
<br>
<%= link_to 'Edit Task', edit_path %>
<%= link_to 'Back to list', tasks_path %>
<%= link_to "Delete Task",
      details_path(@task),
      method: :delete,
      data: { confirm: "Are you sure?" } %>
